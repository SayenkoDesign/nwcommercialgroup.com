{% if images[0].image.sizes is defined %}
    <div class="listing-teaser">
        <a href="{{ link }}">
            <img src="{{ images[0].image.sizes.listing_teaser }}" />
            <div class="description">
                <div class="float-left">
                    <h4>
                        <span>{{ title | raw }}</span>
                        <div class="location">
                            {% if confidential_location %}
                                Confidential Location
                            {% else %}
                                {{ city | raw }}
                                {% if city and state %}, {% endif %}
                                {{ state | raw }}
                            </div>
                        {% endif %}
                    </h4>
                </div>
                <div class="float-right">
                    <div class="price">{{ price | raw }}</div>
                    <div class="cap">CAP RATE {{ cap_rate | raw }}</div>
                </div>
            </div>
        </a>
    </div>
{% endif %}